---
description: Pipeline agent tree removed from this repo
alwaysApply: true
---

# Pipelines / Cursor agents

This deployment **does not** ship a `pipelines/` directory or Cursor-based pipeline agents. Do not reintroduce pipeline-agent automation, `.cursor-pipeline/` trigger flows, or `cursor-agent-run` unless the human explicitly asks to restore that architecture.
