# Single line — nginx includes this inside `location ~ \.php$`.
# Fix 502: run from project root: ./scripts/align-php-fpm-socket.sh
# Or edit manually to match: ls /run/php/*.sock
fastcgi_pass unix:/run/php/php8.3-fpm.sock;
