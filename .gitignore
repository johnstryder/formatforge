zfolder/
configuration.php
.idea
vendor
composer.lock
.env
.vscode
*-pb
pb_data/
.pb-port
.app-port

# Local/system symlinks, vendored trees, runtime data
formatforgeplus
go-highway-src/
storage/
# Pipeline Cursor agent: downloaded copies of backing/generated media (recreated each trigger)
pipelines/**/agent_media/
# Go pipeline binary (build with `go build -o pipeline-generate .` in each pipeline dir)
pipelines/**/pipeline-generate

# Cursor pipeline runtime (PHP recreates dirs on demand; only README.md files are tracked)
.cursor-pipeline/*
!.cursor-pipeline/README.md
!.cursor-pipeline/triggers/
.cursor-pipeline/triggers/*
!.cursor-pipeline/triggers/README.md
=*
